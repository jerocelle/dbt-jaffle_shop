models:
  - name: stg_stripe__payment
    description: One payment per order
    columns:
      - name: payment_id
        description: Primary key
        data_tests:
          - unique
          - not_null